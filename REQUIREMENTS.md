# 纸感护眼：首版需求

## 目标

制作可在 Windows 上运行的中文桌面应用，参考 PaperMan 的全桌面纸张纹理覆盖方式。使用原创界面和程序生成的纹理。

## 本次范围

| 模块 | 必须实现 | 验收方式 |
| --- | --- | --- |
| 纹理 | 细纹纸、棉麻纸、柔雾纸、夜读纸，预览与覆盖采用同一纹理计算 | 逐项切换，确认预览与选中状态更新 |
| 调节 | 纹理强度 0–100、暖色 0–100、压暗 0–50，各自独立 | 修改后确认数值和效果；全部为 0 时完全透明 |
| 开关 | 首次启动默认关闭；开启、关闭；暂停 10 分钟、手动恢复、到期恢复 | 操作开关，检查覆盖层与倒计时；自动测试覆盖时间边界 |
| 显示器 | 所有屏幕或仅主屏幕；响应显示器配置变化 | 本机操作验证；无多屏实机条件的部分标记未实测 |
| 输入 | 覆盖层鼠标穿透、不抢键盘焦点 | 覆盖开启时操作下方控制面板；检查窗口标志 |
| 托盘 | 打开面板、开关、暂停或恢复、退出 | 关闭面板后仍运行；退出后覆盖层和托盘图标消失 |
| 快捷键 | Ctrl+Alt+P 开关，Ctrl+Alt+↑/↓ 调强度 | 实际按键操作；冲突时提示且按钮可用 |
| 提醒 | 默认关闭；20、30、45、60 分钟可选，不抢焦点 | 启用与关闭检查；计时逻辑验证，不伪称等待全部间隔 |
| 设置 | 自动保存，重启恢复；读写失败可理解提示 | 修改后重启，核对选项；自动测试不写入用户实际设置 |
| 生命周期 | 单实例；再次打开显示已有面板；退出释放效果 | 重复启动、正常退出检查 |

## 界面

浅米白背景、深绿点缀、圆角卡片和清晰的中文标签。提供四种纹理选择、阅读预览、独立调节项、当前状态、显示器设置、休息提醒、快捷键说明和明确退出入口。在 1280×800 屏幕内可用，较小可用区域下能够滚动。

## 排除项

账号、支付、联网服务、开机自启、驱动、系统色彩设置修改、按应用排除和跟随鼠标的桌灯效果均不在首版范围。不修改其他应用，不复制原软件素材，不宣称医学疗效。

## 交付与证据

交付源码、Windows 可运行程序、使用说明及真实验证记录。构建和自动测试通过不等于所有实际使用场景已验收；没有实际执行的项目明确标注未实测。
