# MoniPaper v1.2.1 验证范围

## v1.2.1 样式更新

- 阅读预览底色调整为同色系的暖灰米色 `#E5DFD5`。
- 纵向滚动条改为透明轨道、6 像素宽及 60 像素高的圆角滑块，保留 12 像素交互宽度。
- 候选构建完成原有 17 项核心自检、11 个离屏场景及 4 项捕获检查，均通过；预览图片已检查。
- 后续不再追加验证。新版滚动条的实机拖动、滚轮与键盘完整流程未完成验收；最终提交后的发行包仅重新构建，未再次运行测试。
- 包中的 `candidate-self-test-results.json` 来自样式候选构建，不表示对最终重新构建的 EXE 执行了自检。

以下保留 v1.2.0 的功能验收记录，不能视为对本次滚动条样式的追加验证。

## v1.2.0 实机验证

2026-09-02，通过 Windows 桌面控制操作修复后的应用完成以下检查：

| 检查 | 结果 | 实际证据 |
| --- | --- | --- |
| 录制已经注册的组合键 | 通过 | 将呼出面板键录成现有的 Ctrl+Alt+P，主键被完整捕获 |
| 重复组合拒绝应用 | 通过 | 显示具体冲突，点击应用后仍在编辑页；配置文件 SHA-256 未改变 |
| 有效自定义与保存 | 通过 | 将呼出键改为 Ctrl+Alt+Shift+O，应用成功，摘要及设置文件均更新 |
| 自定义键呼出主面板 | 通过 | 在更多设置页按自定义组合，返回并激活主面板 |
| 重启保留设置 | 通过 | 重启后仍显示自定义组合及退出程序的关闭选项 |
| 恢复默认与释放旧键 | 通过 | 默认 Ctrl+Alt+O 恢复生效；旧的 Ctrl+Alt+Shift+O 不再呼出面板 |
| 关闭窗口时退出 | 通过 | 选择退出程序后点击窗口关闭按钮，进程结束 |
| 关闭窗口时最小化到托盘 | 通过 | 窗口隐藏，进程仍运行；再次启动程序恢复同一个进程的主面板 |
| 显式退出 | 通过 | 托盘偏好下点击退出程序，窗口及进程正常结束 |

测试结束后恢复默认四个组合键及 `CloseToTray=true`；原有八项滤镜、屏幕和提醒参数与测试前保存值一致。实机测试使用的程序集与待发布源码构建出的程序集校验值一致。

初次实测发现 Windows 已注册快捷键会拦截普通 WPF 按键事件。修复后使用仅录制期间存在的捕获会话，并重新执行了上述真实录制和改绑检查。

## 新增自动验证

- 核心自检共 17 项：原有 6 项，加上快捷键校验、旧配置兼容、配置往返、坏快捷键字段保留旧参数、改绑、内部互换、注册冲突、保存失败保留旧键、呼出动作分发、释放及真实 Win32 注册冲突等检查。
- 离屏界面共 11 个场景，包含编辑页、重复错误、长组合小视口和两种关闭行为。
- 捕获验证共 4 项，覆盖按键/重复/释放序列、取消与非法导航组合、Tab/失焦，以及真实捕获接口安装与释放。
- 捕获序列测试不向桌面发送模拟按键；真实改绑操作由上方的实机验证单独记录。

## 界面基础与一致性

按 `better-layout → minimalist-ui → better-writing → better-ui` 的顺序进行改版，具体 WPF 适配规则见 `DESIGN.md`。

| 级别 | 位置 | 改版前 | 改版后 | 验证 |
| --- | --- | --- | --- | --- |
| MEDIUM | 主面板布局 | 双栏卡片和设置同时展开 | 约 460×560 单列面板，低频设置进入第二页 | 离屏视觉检查及布局断言通过 |
| MEDIUM | 控件与状态 | 方框总开关，暂停、恢复同时出现 | 独立总开关，暂停与恢复共用一个位置 | 未开启、开启、暂停三种呈现通过 |
| MEDIUM | 页面文字 | 多段功能和实现说明 | 保留简短标签、预览与必要提示 | 文案与现有动作对照检查通过 |
| LOW | 图标、选项和滑条 | 默认控件样式及大卡片 | 清晰纸页图标、紧凑纸张选择、细滑轨和分段选项 | 默认、更多设置及最小视口预览已检查 |

原有 6 个离屏场景保留：默认、已开启、暂停、更多设置、最小视口、长错误提示。默认内容视口为 460×520，最小内容视口为 400×500；新增控件沿用相同样式。离屏检查不替代 Windows 系统缩放验证。

离屏模式在读取用户设置、注册快捷键或创建托盘之前执行，不显示窗口。验证前后用户设置文件的 SHA-256 一致。`docs/images` 中保存了主面板与更多设置的示例预览。

结论：上述限定范围 Approve；未覆盖的系统环境和交互见末节。

## 图标验证

沿用 v1.0.1 的纸页图标；v1.1.0 主面板改用同一设计的 PNG 高质量缩放。

- PNG 为无眼睛元素的折角纸页图标，透明背景保留。
- ICO 包含 16、20、24、32、40、48、64、128、256 像素九种尺寸。
- 已检查 16、32、48、256 像素在浅色和深色背景上的图形表现。
- v1.0.1 已使用 `ExtractIconEx` 从发布程序读取图标资源，图标组数量为 1，大、小图标句柄均有效；本次保持同一 ICO 引用。
- 程序文件、WPF 窗口和托盘引用同一套图标。未操作 Windows 图标缓存，也未把资源检查表述为实际桌面交互验收。

## 构建与自检

Windows x64 自包含发布包执行 17 项核心自检，发布包内的 `self-test-results.json` 记录真实结果。以下是保留的 6 项基础检查，新增项目见上文。

| 检查 | 范围 |
| --- | --- |
| settings-normalize | 无效设置值限制与提醒间隔规范化 |
| settings-json-roundtrip | 隔离临时文件中的设置 JSON 往返，不覆盖用户设置 |
| pause-boundary | 暂停结束前一刻及到期时刻的状态 |
| texture-transparent-and-premultiplied | 全 0 时完全透明；像素满足预乘 alpha 约束 |
| display-enumeration | Windows 显示器枚举与主屏幕存在性 |
| small-overlay-lifecycle | 实际创建 64×48 覆盖窗口，检查 layered、transparent、no-activate、tool-window 标志及销毁 |

可以运行 `build-release.ps1` 重新发布并执行这些检查。

## 尚未完成的实机验证

本次实机通过范围不代表所有环境均已覆盖。以下项目仍未完成完整实机验证：

- 不同系统缩放下的完整布局、所有纹理与滑条组合。
- 多个第三方程序、管理员窗口及低级键盘钩子环境下的快捷键兼容性。
- 托盘右键菜单的全部操作、暂停倒计时完整到期过程。
- 等待完整 20–60 分钟的休息提醒。
- 多显示器视觉效果、不同缩放比例及显示器热插拔。
- HDR、独占全屏、受保护的视频画面和 Windows 安全桌面。

上述项目均不能用构建成功或窗口标志检查替代。
