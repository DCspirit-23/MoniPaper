# v1.1.0 验证范围

## 界面改版检查

按 `better-layout → minimalist-ui → better-writing → better-ui` 的顺序进行改版，具体 WPF 适配规则见 `DESIGN.md`。

| 级别 | 位置 | 改版前 | 改版后 | 验证 |
| --- | --- | --- | --- | --- |
| MEDIUM | 主面板布局 | 双栏卡片和设置同时展开 | 约 460×560 单列面板，低频设置进入第二页 | 离屏视觉检查及布局断言通过 |
| MEDIUM | 控件与状态 | 方框总开关，暂停、恢复同时出现 | 独立总开关，暂停与恢复共用一个位置 | 未开启、开启、暂停三种呈现通过 |
| MEDIUM | 页面文字 | 多段功能和实现说明 | 保留简短标签、预览与必要提示 | 文案与现有动作对照检查通过 |
| LOW | 图标、选项和滑条 | 默认控件样式及大卡片 | 清晰纸页图标、紧凑纸张选择、细滑轨和分段选项 | 默认、更多设置及最小视口预览已检查 |

离屏渲染覆盖 6 个场景：默认、已开启、暂停、更多设置、最小视口、长错误提示。每个场景的布局和状态断言均通过。默认内容视口为 460×520，最小内容视口为 400×500，预留了原生窗口标题栏的空间；这不是 Windows 实际缩放验证。

离屏模式在读取用户设置、注册快捷键或创建托盘之前执行，不显示窗口。验证前后用户设置文件的 SHA-256 一致。`docs/images` 中保存了主面板与更多设置的示例预览。

结论：上述离屏布局和状态范围 Approve；实际桌面交互 Not verified。

## 图标验证

沿用 v1.0.1 的纸页图标；v1.1.0 主面板改用同一设计的 PNG 高质量缩放。

- PNG 为无眼睛元素的折角纸页图标，透明背景保留。
- ICO 包含 16、20、24、32、40、48、64、128、256 像素九种尺寸。
- 已检查 16、32、48、256 像素在浅色和深色背景上的图形表现。
- v1.0.1 已使用 `ExtractIconEx` 从发布程序读取图标资源，图标组数量为 1，大、小图标句柄均有效；本次保持同一 ICO 引用。
- 程序文件、WPF 窗口和托盘引用同一套图标。未操作 Windows 图标缓存，也未把资源检查表述为实际桌面交互验收。

## 构建与自检

Windows x64 自包含发布包已完成构建，并通过以下 6 项自动检查。发布包内的 `self-test-results.json` 记录该包的真实执行结果。

| 检查 | 范围 |
| --- | --- |
| settings-normalize | 无效设置值限制与提醒间隔规范化 |
| settings-json-roundtrip | 隔离临时文件中的设置 JSON 往返，不覆盖用户设置 |
| pause-boundary | 暂停结束前一刻及到期时刻的状态 |
| texture-transparent-and-premultiplied | 全 0 时完全透明；像素满足预乘 alpha 约束 |
| display-enumeration | Windows 显示器枚举与主屏幕存在性 |
| small-overlay-lifecycle | 实际创建 64×48 覆盖窗口，检查 layered、transparent、no-activate、tool-window 标志及销毁 |

可以运行 `build-release.ps1` 重新发布并执行这些检查。

## 尚未完成的实机验证

自动测试不代表全部界面流程验收通过。截至 v1.1.0，以下项目未完成实机交互验证：

- 真实 Windows 窗口、系统缩放下的布局，以及纹理选择与滑条的实际操作。
- 全屏覆盖启用后的实际鼠标穿透及快捷键操作。
- 暂停及恢复按钮、托盘菜单、单实例面板恢复与正常退出流程。
- 修改实际用户设置后退出、重新启动的恢复效果。
- 等待完整 20–60 分钟的休息提醒。
- 多显示器视觉效果、不同缩放比例及显示器热插拔。
- HDR、独占全屏、受保护的视频画面和 Windows 安全桌面。

上述项目均不能用构建成功或窗口标志检查替代。
